<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.deway.blog.mapper.BlogMapper">
    <sql id="baseMap">
        id as id, author as author, create_time as createTime, update_time as updateTime,
        cateGory as cateGory, favour as favour
    </sql>

    <select id="list" resultType="com.deway.blog.entiry.Blog">
        select <include refid="baseMap"/>
        from blog
    </select>

</mapper>